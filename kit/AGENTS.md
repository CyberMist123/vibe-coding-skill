# {{PROJECT_NAME}} — Agent Entry

开始任何工作前：

1. 先读根目录 [`PROJECT.md`](./PROJECT.md)。它是需求、边界、架构、接口、数据所有权、进度和下一步的唯一当前事实入口。
2. 再只读与当前任务直接相关的代码和详细文档；不要为了接手而重新扫描整个仓库。
3. 明确当前事项属于：`计划中`、`已实现/未验证`、`已验证`、`暂停` 或 `放弃`。

完成工作后：

1. 若改变了需求、验收条件、边界、架构、模块职责、接口名、数据所有权、运行流程或进度，执行 [`skills/project-doc-sync/SKILL.md`](./skills/project-doc-sync/SKILL.md)。
2. 先原地更新 `PROJECT.md`，再更新其中“文档所有权图”列出的受影响详细文档与当前 Issue。
3. 删除陈旧事实，不新增重复的 `STATUS.md`、`PROJECT_STATE.md`、handoff 副本或第二套架构说明。
4. 没有真实测试、日志、目标环境输出或人工 smoke 时，不得声称功能已经验证。

硬边界：

- 用户确认的产品边界和 `PROJECT.md` 中的不变量优先于模型自行扩展。
- 不提交密钥、凭据、私密数据、运行数据库、媒体、日志或备份。
- 不执行破坏性命令、数据迁移或生产写操作，除非任务明确要求且已具备回滚方案。
- 不因“顺手优化”引入新框架、第二套服务、重复模块或无关依赖。
- 失败时只修已证实的失败点；不要借机全库重构。
- 任务完成后按项目事实报告真实状态，不用会话总结代替项目文档。
